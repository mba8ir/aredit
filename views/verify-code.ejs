<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>رمز التحقق | راية الفرسان</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Noto+Kufi+Arabic:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include('partials/header') %>
  <main class="container">
    <div class="auth-card">
      <h1>رمز التحقق</h1>
      <p style="color: #aaa; font-size: 0.85rem; margin-bottom: 1rem;">أدخل رمز التحقق المكون من 6 أرقام</p>
      <% if (error) { %>
        <div class="alert alert-error"><%= error %></div>
      <% } %>
      <form method="POST" action="/verify-code/<%= token %>">
        <div class="form-group">
          <label for="code">رمز التحقق</label>
          <input type="text" id="code" name="code" required maxlength="6" pattern="\d{6}" dir="ltr" placeholder="000000" style="text-align: center; font-size: 1.5rem; letter-spacing: 0.5rem;">
        </div>
        <button type="submit" class="btn btn-primary btn-block">تحقق</button>
      </form>
      <p class="auth-switch"><a href="/forgot-password">إعادة إرسال الرمز</a></p>
    </div>
  </main>
</body>
</html>
