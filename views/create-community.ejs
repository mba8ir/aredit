<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>مجتمع جديد | راية الفرسان</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Noto+Kufi+Arabic:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include('partials/header') %>
  <main class="container">
    <div class="create-post-card">
      <h1>إنشاء مجتمع جديد</h1>
      <% if (error) { %>
        <div class="alert alert-error"><%= error %></div>
      <% } %>
      <form method="POST" action="/c/new" enctype="multipart/form-data">
        <div class="form-group">
          <label for="name">اسم المجتمع (بالعربية)</label>
          <input type="text" id="name" name="name" required dir="rtl" placeholder="مثال: التقنية العربية" maxlength="50">
        </div>
        <div class="form-group">
          <label for="description">الوصف (اختياري)</label>
          <textarea id="description" name="description" rows="3" dir="rtl" placeholder="وصف مختصر عن المجتمع..." maxlength="500"></textarea>
        </div>
        <div class="form-group">
          <label for="rules">قوانين المجتمع (اختياري)</label>
          <textarea id="rules" name="rules" rows="4" dir="rtl" placeholder="1. احترام الآخرين&#10;2. عدم النشر المكرر&#10;3. ..." maxlength="2000"></textarea>
        </div>
        <div class="form-group">
          <label for="icon">أيقونة المجتمع (إيموجي)</label>
          <input type="text" id="icon" name="icon" placeholder="🕌" maxlength="4" style="width: 80px; text-align: center; font-size: 1.5rem;">
        </div>
        <div class="form-group">
          <label for="accent_color">لون المجتمع</label>
          <input type="color" id="accent_color" name="accent_color" value="#e94560" style="width: 60px; height: 40px; padding: 2px; cursor: pointer;">
        </div>
        <div class="form-group">
          <label for="banner">صورة الغلاف (اختياري)</label>
          <input type="file" id="banner" name="banner" accept="image/*">
          <div class="file-preview" id="banner-preview"></div>
        </div>
        <button type="submit" class="btn btn-primary btn-block">إنشاء المجتمع</button>
      </form>
    </div>
  </main>
  <script>
    document.getElementById('banner').addEventListener('change', function() {
      const preview = document.getElementById('banner-preview');
      if (this.files && this.files[0]) {
        const reader = new FileReader();
        reader.onload = function(e) {
          preview.innerHTML = '<img src="' + e.target.result + '" style="max-width:100%;max-height:200px;border-radius:8px;margin-top:0.5rem;">';
        };
        reader.readAsDataURL(this.files[0]);
      }
    });
  </script>
</body>
</html>
