<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageTitle %> | راية الفرسان</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Noto+Kufi+Arabic:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include('partials/header') %>
  <main class="container">
    <h1 class="section-title" style="margin-bottom: 1.5rem;"><% if (q) { %>نتائج البحث عن: "<%= q %>"<% } else { %>البحث<% } %></h1>

    <form action="/search" method="GET" class="search-page-form">
      <input type="text" name="q" value="<%= q %>" placeholder="ابحث عن منشورات..." class="search-page-input" autofocus>
      <button type="submit" class="btn btn-primary">بحث</button>
    </form>

    <% if (q && posts.length === 0) { %>
      <p class="empty-state">لا توجد نتائج لـ "<%= q %>"</p>
    <% } %>

    <% posts.forEach(post => { %>
      <%- include('partials/post-card', { post, userVotes: {} }) %>
    <% }) %>

    <%- include('partials/pagination', { page, totalPages, q }) %>
  </main>
  <div id="toast-container"></div>
  <script src="/app.js"></script>
</body>
</html>
