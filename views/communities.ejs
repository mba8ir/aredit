<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>المجتمعات | راية الفرسان</title>
  <meta name="description" content="تصفح جميع المجتمعات في راية الفرسان">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Noto+Kufi+Arabic:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include('partials/header') %>
  <main class="container">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
      <h1 class="section-title" style="margin-bottom: 0;">جميع المجتمعات (<%= communities.length %>)</h1>
      <% if (currentUser) { %>
        <a href="/c/new" class="btn btn-primary">+ مجتمع جديد</a>
      <% } %>
    </div>

    <div class="community-grid">
      <% communities.forEach(c => { %>
        <a href="/c/<%= c.id %>" class="community-grid-card">
          <span class="community-grid-icon"><%= c.icon %></span>
          <span class="community-grid-name"><%= c.name %></span>
          <% if (c.description) { %>
            <span class="community-grid-desc"><%= c.description.substring(0, 100) %></span>
          <% } %>
          <span class="community-grid-followers"><%= c.follower_count %> متابع</span>
        </a>
      <% }) %>
    </div>
  </main>
  <script src="/app.js"></script>
</body>
</html>
