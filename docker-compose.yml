version: '3.8'

services:
  rayat-al-fursan:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./rayat.db:/app/rayat.db
      - ./uploads:/app/uploads
    env_file:
      - .env
    restart: unless-stopped
